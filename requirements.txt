# Greenfield dependency entrypoint.
# The authoritative dependency graph and versions are in pyproject.toml and
# uv.lock. This compatibility file installs the project with test tooling for
# local validation; production images should install the project without the
# test extra and use the lockfile in their build process.
-e .[test]
