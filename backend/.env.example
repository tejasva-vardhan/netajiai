# Set these in the repo-root `.env` (next to `main.py`) or in your host environment (Render).
# Never commit real secrets.

ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
ADMIN_API_KEY=
AUTH_SECRET_KEY=
# AUTH_TOKEN_TTL_MINUTES=10080

DATABASE_URL=postgresql+psycopg://user:password@host:5432/aineta

GROQ_API_KEY=
MODEL_NAME=llama-3.3-70b-versatile

SMTP_USERNAME=
SMTP_PASSWORD=
TEST_RECEIVER_EMAIL=
MAIL_FROM=
DEV_SAFE_INBOX=

# DEFAULT_CITY_ID=
# DEFAULT_ADMIN_EMAIL=
# DEFAULT_ADMIN_DEPARTMENT_NAME=
