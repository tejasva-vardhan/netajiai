# Greenfield dependency entrypoint when invoked from backend/.
# The authoritative dependency graph and versions are in ../pyproject.toml and
# ../uv.lock. Production images should use the lockfile directly.
-e ..[test]
